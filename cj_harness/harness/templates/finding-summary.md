# 취약점 발견 요약 — UNSET

어떤 요청을 보냈고, 어디서 어떻게 취약점이 발견됐는지를 정리한 사람이 읽기 쉬운 요약본.
원본 요청/응답 원문(raw)은 git에 올리지 않는 정책이라 이 파일을 대신 커밋 가능한
위치(`evidence/`)에 둔다.

## 대상 (Target)

| 항목 | 값 |
| --- | --- |
| 취약 버전 | UNSET |
| 패치 버전 | UNSET |

## 보낸 요청

```
UNSET
```

## 어디서 취약점이 발견됐나 (근본 원인 위치)

UNSET

## 결과

| Target | 응답 | 판정 |
| --- | --- | --- |
| UNSET | UNSET | UNSET |

## 관련 파일

- `all-requests.md`, `all-results.md`
- `same-control-check.md`
- `../analysis/root-cause.md`
