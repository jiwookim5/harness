# 요청 결과 전체 (All Results) — UNSET

`all-requests.md`에 기록된 각 요청의 결과를 같은 순서·같은 번호로 대응해서, 생략 없이
기록한다. 이 CVE의 `evidence/index.json`에 등록된 evidence_id는 전부 여기 한 번씩
등장해야 한다.

## 결과 1 — UNSET (evidence_id: UNSET)

```
HTTP_STATUS: UNSET
```

응답 본문 (raw 그대로):
```
UNSET
```

- 판정: UNSET
- raw 원본 위치(로컬 전용, git 미추적): `evidence/raw/UNSET`

## 관련 파일

- 보낸 요청: `evidence/all-requests.md`
- 원본 구조화 기록: `execution/vulnerable.json`, `execution/patched.json`
